<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="/main.css" rel="stylesheet">
  <title>Document</title>
</head>
<body>
  <%- include('nav') %>
  <% for (let i = 0; i < classInfo.length; i++) { %>
    <div class="class-item">
        <a href="/classDetailPage/<%= classInfo[i]._id %>"
          onclick="joinRoom('<%= classInfo[i]._id %>')"><%= classInfo[i].className %></a>
    </div>
  <% } %>
</body>
</html>
